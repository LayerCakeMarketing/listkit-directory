--- a/app/Services/CloudflareImageService.php
+++ b/app/Services/CloudflareImageService.php
@@ -33,9 +33,8 @@ class CloudflareImageService
     {
         try {
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
                 'Content-Type' => 'application/json',
             ])->post("{$this->baseUrl}/direct_upload", [
                 'expiry' => now()->addMinutes(30)->toISOString(), // 30 minute expiry
@@ -80,9 +79,8 @@ class CloudflareImageService
 
             // Prepare the request
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
             ])->attach(
                 'file', 
                 file_get_contents($file->getRealPath()), 
@@ -122,9 +120,8 @@ class CloudflareImageService
     {
         try {
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
             ])->delete("{$this->baseUrl}/{$imageId}");
 
             return $response->successful();
@@ -210,9 +207,8 @@ class CloudflareImageService
     {
         try {
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
             ])->get("{$this->baseUrl}/{$imageId}");
 
             if (!$response->successful()) {
@@ -234,9 +230,8 @@ class CloudflareImageService
     {
         try {
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
             ])->get("{$this->baseUrl}/stats");
 
             if ($response->successful()) {
@@ -256,9 +251,8 @@ class CloudflareImageService
     {
         try {
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
             ])->get("https://api.cloudflare.com/client/v4/accounts/{$this->accountId}/images/v1/variants");
 
             if (!$response->successful()) {
@@ -283,9 +277,8 @@ class CloudflareImageService
         try {
             // Note: Cloudflare uses PATCH to create/update variants
             $response = Http::withHeaders([
-                'X-Auth-Email' => $this->email,
-                'X-Auth-Key' => $this->apiToken,
+                'Authorization' => 'Bearer ' . $this->apiToken,
                 'Content-Type' => 'application/json',
             ])->patch("https://api.cloudflare.com/client/v4/accounts/{$this->accountId}/images/v1/variants/{$id}", [
                 'id' => $id,
--- a/app/Http/Controllers/Admin/MediaController.php
+++ b/app/Http/Controllers/Admin/MediaController.php
@@ -424,9 +424,8 @@ class MediaController extends Controller
             }
 
             $response = Http::withHeaders([
-                'X-Auth-Email' => $email,
-                'X-Auth-Key' => $apiToken,
+                'Authorization' => 'Bearer ' . $apiToken,
             ])->timeout(30)->get("https://api.cloudflare.com/client/v4/accounts/{$accountId}/images/v1", [
                 'page' => $page,
                 'per_page' => $perPage
@@ -621,9 +620,8 @@ class MediaController extends Controller
             $email = config('services.cloudflare.email');
             
             $response = Http::withHeaders([
-                'X-Auth-Email' => $email,
-                'X-Auth-Key' => $apiToken,
+                'Authorization' => 'Bearer ' . $apiToken,
             ])->get("https://api.cloudflare.com/client/v4/accounts/{$accountId}/images/v1/{$imageId}");
 
             if (!$response->successful()) {